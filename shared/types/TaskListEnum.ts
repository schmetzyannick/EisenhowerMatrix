/**
 * Enum that contains all the possible task list names.
 */
export enum TaskListEnum {
    Backlog = 'Backlog',
    Done = 'Done',
    Now = 'Now',
    Delegate = 'Delegate',
    Own = 'Own',
    Trash = 'Trash',
}